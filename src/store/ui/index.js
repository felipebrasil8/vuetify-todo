import { state } from '@/store/ui/ui.state';
import { mutations } from '@/store/ui/ui.mutations';
import { actions } from '@/store/ui/ui.actions';
import { getters } from '@/store/ui/ui.getters';

export const ui = {
  state,
  mutations,
  actions,
  getters,
}